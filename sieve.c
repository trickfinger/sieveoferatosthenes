#include <stdio.h>
#include <math.h>
#define MAX 1000000

int main()
{

	unsigned int crivo[MAX];
	double num;
	int i, j, result;

		scanf("%lf", &num);
	for(i=0;i<num;i++)
		
	crivo[MAX] = 0;
		aux = floor(sqrt(num);

	for(i=2;i<aux;i++) {
		if(crivo[MAX] == 0)
	crivo[MAX] = 1;

	for(j=i+i;j<MAX;j = j + i)
	crivo[MAX] = 2;
}
	result = 0;

	for(i=2;i<MAX;i++)
		if(crivo[MAX] == 1)
	result = result + i;
		printf("%d", result);

return (0);
}
